/**
 * Motion Features (Lazy Load)
 *
 * domMax는 Motion의 모든 기능(애니메이션, 제스처, 드래그, 레이아웃 등)을 포함
 * 별도 파일로 분리하여 필요할 때만 비동기적으로 불러오도록(Lazy Load) 설정
 */
import { domMax } from 'motion/react';

export default domMax;
